apiVersion: apps/v1
kind: Deployment
metadata:
  name: golang-prod
  namespace: golang-prod
  labels:
    app: golang-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: golang-prod
  template:
    metadata:
      labels:
        app: golang-prod
    spec:
      containers:
      - name: go-hello-cnt
        image: varu33333/golan:replace_me
      imagePullSecrets:
      - name: regcred02
---
apiVersion: v1
kind: Service
metadata:
  name: go-srv
  namespace: golang-prod
spec:
  ports:
    - name: tcp-8080-8080
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: golang-prod
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-ingress
  namespace: golang-prod
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: go-prod.strategy22.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: go-srv
            port:
              number: 8080